apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: loki

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - pvc.yaml
  - sealedsecret.json
  - deployment.yaml

configMapGenerator:
  - name: loki
    files:
      - conf/loki.yaml